#!/bin/bash
# @author: Gia Tuấn
# @website: https://wptangtoc.com
# @email: giatuan@wptangtoc.com

function huong_dan() {
  Tính năng cập nhật WPTangToc OLS là một chức năng quan trọng giúp người dùng dễ dàng nâng cấp script WPTangToc OLS lên phiên bản mới nhất.

  Giới thiệu ngắn gọn:
  Mục đích: Đảm bảo người dùng luôn được sử dụng phiên bản WPTangToc OLS mới nhất với các bản vá lỗi, cải tiến hiệu suất, tính năng mới và cập nhật bảo mật cũng như các thành phần liên quan.

  wptangtoc ols tích hợp sẵn tính năng cập nhật trong menu tự động kiểm tra phiên bản mới nhất từ nguồn phát hành chính thức.
  Người dùng chỉ cần chạy lệnh cập nhật đơn giản, wptangtoc ols sẽ tự động xử lý các bước cần thiết.

  Lợi ích:
  * An toàn và ổn định: Nhận các bản vá lỗi và cập nhật bảo mật mới nhất.
  * Hiệu suất tối ưu: Hưởng lợi từ các cải tiến về tốc độ và tài nguyên.
  * Tính năng mới: Trải nghiệm các công cụ và tùy chọn quản lý máy chủ mới được bổ sung.
  * Tiện lợi: Quá trình cập nhật được tự động hóa, tiết kiệm thời gian và công sức.

  Việc thường xuyên kiểm tra và thực hiện cập nhật WPTangToc OLS giúp máy chủ web của bạn hoạt động hiệu quả, an toàn và luôn được hỗ trợ tốt nhất.
}

PATH='/root/.local/bin:/root/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin'

unset khong_dau_vi_ngon_ngu wptangtocols_version ngon_ngu
. /etc/wptt/.wptt.conf 2>/dev/null
[[ -z "$ngon_ngu" ]] && ngon_ngu='vi'
. "/etc/wptt/lang/$ngon_ngu.sh" 2>/dev/null
. /etc/wptt/echo-color 2>/dev/null
. /etc/wptt/core-functions 2>/dev/null

# ==============================================================================
# CHỐT CHẶN HỆ ĐIỀU HÀNH CENTOS 7
# ==============================================================================
if grep -qi 'ID="centos"' /etc/os-release 2>/dev/null && grep -qE 'VERSION_ID="?7"?' /etc/os-release 2>/dev/null; then
  echo -e "\n${C_RED}╭──────────────────────────────────────────────────────────────────────────────╮${C_RESET}"
  echo -e "${C_RED}│${C_RESET} ${C_YELLOW}CẢNH BÁO: HỆ ĐIỀU HÀNH KHÔNG CÒN ĐƯỢC HỖ TRỢ${C_RESET}                               ${C_RED}│${C_RESET}"
  echo -e "${C_RED}├──────────────────────────────────────────────────────────────────────────────┤${C_RESET}"
  echo -e "${C_RED}│${C_RESET} Tính năng cập nhật này đã chính thức ngừng hoạt động trên ${C_BOLD_WHITE}CentOS 7${C_RESET}.      ${C_RED}│${C_RESET}"
  echo -e "${C_RED}│${C_RESET} Hiện tại CentOS 7 đã hết vòng đời (End-of-Life) và không còn an toàn.      ${C_RED}│${C_RESET}"
  echo -e "${C_RED}│${C_RESET} Vui lòng chuyển qua sử dụng ${C_GREEN}AlmaLinux 9${C_RESET} để tiếp tục nhận bản cập nhật! ${C_RED}│${C_RESET}"
  echo -e "${C_RED}╰──────────────────────────────────────────────────────────────────────────────╯${C_RESET}\n"
  exit 1
fi
# ==============================================================================
# CHỐT CHẶN HỆ ĐIỀU HÀNH CENTOS 7
# ==============================================================================

box_inner_width=78
print_line() {
  local text="$1"
  local clean_text=$(echo -e "$text" | sed "s/\x1B\[[0-9;]*[a-zA-Z]//g")
  local text_len=${#clean_text}
  local pad=$((box_inner_width - text_len - 1))
  [[ $pad -lt 0 ]] && pad=0
  echo -e "${C_CYAN}│${C_RESET} ${text}$(printf '%*s' "$pad" "")\e[K${C_CYAN}│${C_RESET}"
}

center_text() {
  local text="$1"
  local clean_text=$(echo -e "$text" | sed "s/\x1B\[[0-9;]*[a-zA-Z]//g")
  local text_len=${#clean_text}
  if ((text_len >= box_inner_width)); then
    printf "${C_CYAN}│${C_RESET} %b ${C_CYAN}│${C_RESET}\n" "$text"
    return
  fi
  local pad_left=$(((box_inner_width - text_len) / 2))
  local pad_right=$((box_inner_width - text_len - pad_left))
  printf "${C_CYAN}│${C_RESET}%*s%b%*s${C_CYAN}│${C_RESET}\n" "$pad_left" "" "$text" "$pad_right" ""
}

clear

# ==============================================================================
# 1. KIỂM TRA PHIÊN BẢN (GIAO DIỆN CHỜ & DỰ PHÒNG GITHUB)
# ==============================================================================
echo -e "${C_CYAN}╭──────────────────────────────────────────────────────────────────────────────╮${C_RESET}"
center_text "${C_YELLOW}HỆ THỐNG ➜ CẬP NHẬT WPTANGTOC OLS${C_RESET}"
echo -e "${C_CYAN}├──────────────────────────────────────────────────────────────────────────────┤${C_RESET}"
print_line "${C_BOLD_WHITE}Phiên bản hiện tại:${C_RESET} ${C_YELLOW}$version_wptangtoc_ols${C_RESET}"
print_line "${C_CYAN}Đang kết nối máy chủ để kiểm tra phiên bản mới nhất...${C_RESET}"
echo -e "${C_CYAN}╰──────────────────────────────────────────────────────────────────────────────╯${C_RESET}"

# Lấy phiên bản mới nhất từ máy chủ chính
wptangtocols_version=$(curl -sL --connect-timeout 5 --max-time 10 https://wptangtoc.com/share/version-wptangtoc-ols.txt?domain-update="$Website_chinh")

# Trạm dự phòng: Gọi thẳng API từ Github nếu máy chủ chính sập hoặc trả về rỗng
[[ -z "$wptangtocols_version" ]] && wptangtocols_version=$(curl -sL --connect-timeout 5 --max-time 10 https://raw.githubusercontent.com/wptangtoc/wptangtoc-ols/refs/heads/main/version-wptangtoc-ols.txt)

if [[ -z "$wptangtocols_version" ]]; then
  echo -e "\n${C_RED}Lỗi mạng: Không thể kết nối đến máy chủ WPTangToc và trạm dự phòng. Vui lòng thử lại sau!${C_RESET}"
  sleep 3
  [[ ${1:-} == "98" ]] && exec /etc/wptt/wptt-update-main 1
  [[ ${1:-} == "97" ]] && exec /usr/bin/wptangtoc 1
  exec /etc/wptt/wptt-status2
  exit
fi

# ==============================================================================
# 2. XỬ LÝ LOGIC PHIÊN BẢN
# ==============================================================================
if [[ "$wptangtocols_version" == "$version_wptangtoc_ols" ]]; then
  echo -e "\n${C_GREEN}╭──────────────────────────────────────────────────────────────────────────────╮${C_RESET}"
  center_text "${C_BOLD_WHITE}BẠN ĐANG SỬ DỤNG PHIÊN BẢN MỚI NHẤT${C_RESET}"
  echo -e "${C_GREEN}╰──────────────────────────────────────────────────────────────────────────────╯${C_RESET}"
  sleep 2
  [[ ${1:-} == "98" ]] && exec /etc/wptt/wptt-update-main 1
  [[ ${1:-} == "97" ]] && exec /usr/bin/wptangtoc 1
  exec /etc/wptt/wptt-status2
else
  # ============================================================================
  # DÙNG THƯ VIỆN GIAO DIỆN ĐỂ HỎI NGƯỜI DÙNG CÓ MUỐN CẬP NHẬT HAY KHÔNG
  # ============================================================================
  if wptt_xac_nhan "Đã có bản cập nhật mới! Bạn có muốn nâng cấp ngay không?" "${C_BOLD_WHITE}Bản hiện tại:${C_RESET} ${C_YELLOW}$version_wptangtoc_ols${C_RESET}" "${C_BOLD_WHITE}Bản mới nhất:${C_RESET} ${C_GREEN}$wptangtocols_version${C_RESET}" "Tiến hành nâng cấp" "Để sau"; then
    echo ""

    OLD_PWD=$(pwd)

	if [[ ! -d /etc/wptt/tmp ]];then
		mkdir -p /etc/wptt/tmp
		chmod 700 /etc/wptt/tmp
	fi

	TMP_DIR=$(mktemp -d /etc/wptt/tmp/XXXXXX)
    cd "$TMP_DIR" || exit

    # ==========================================================================
    # BỘ MÁY XÁC THỰC BẢO MẬT GPG & FINGERPRINT PINNING (CHỐNG TRÁO HÀNG)
    # ==========================================================================
    # 1. Tải Public Key (Nếu chưa có, chỉ cần tải 1 lần lưu vào hệ thống)
    if ! gpg --list-keys "giatuan@wptangtoc.com" >/dev/null 2>&1; then
      _runing "Đang nạp Khóa bảo mật GPG..."
      
      # Tải file key về thư mục tạm
      curl -sL --connect-timeout 10 "https://wptangtoc.com/share/wptangtoc-gpg-pub.key" -o wptangtoc-gpg-pub.key
      
      # Dự phòng Github nếu web chính sập
      if [[ ! -s wptangtoc-gpg-pub.key ]]; then
        curl -sL --connect-timeout 10 "https://raw.githubusercontent.com/wptangtoc/wptangtoc-ols/refs/heads/main/wptangtoc-gpg-pub.key" -o wptangtoc-gpg-pub.key
      fi

      # Đọc Fingerprint thực tế (Chỉ lấy dòng đầu của Master Key)
      real_fp=$(gpg --show-keys --with-colons wptangtoc-gpg-pub.key 2>/dev/null | awk -F: '$1 == "fpr" {print $10}' | head -n 1)
      
      # Fingerprint
      expected_fp="EE091EEB9A5189BC80837C059AA528D37B902267"

      # Kiểm định khắt khe
      if [[ -n "$real_fp" && "$real_fp" == "$expected_fp" ]]; then
        gpg --import wptangtoc-gpg-pub.key >/dev/null 2>&1
        _rundone "Nạp Khóa bảo mật GPG thành công"
      else
        _runloi "CẢNH BÁO: Public Key giả mạo hoặc sai lệch Fingerprint!"
        rm -f wptangtoc-gpg-pub.key
        cd "$OLD_PWD" || cd /root || exit
        rm -rf "${TMP_DIR:?}"
        sleep 4
        [[ ${1:-} == "98" ]] && exec /etc/wptt/wptt-update-main 1
        [[ ${1:-} == "97" ]] && exec /usr/bin/wptangtoc 1
        exec /etc/wptt/wptt-status2
        exit 1
      fi
      rm -f wptangtoc-gpg-pub.key
    fi

    # 2. Tải File Core (.zip) và File Chữ Ký (.sig)
    _runing "Đang tải mã nguồn Core WPTangToc OLS"
    wget -q --timeout=15 --tries=3 "https://wptangtoc.com/share/wptangtoc-ols.zip" -O wptangtoc-ols.zip
    wget -q --timeout=15 --tries=3 "https://hub.wptangtoc.com/wptangtoc-ols.zip.sig" -U 'checksum wptangtoc ols' -O wptangtoc-ols.zip.sig
    
    # Dự phòng Github nếu máy chủ sập hoặc file trả về rỗng (0 byte)
    if [[ ! -s wptangtoc-ols.zip || ! -s wptangtoc-ols.zip.sig ]]; then
        wget -q --timeout=15 --tries=3 "https://raw.githubusercontent.com/wptangtoc/wptangtoc-ols/refs/heads/main/wptangtoc-ols.zip" -O wptangtoc-ols.zip
        wget -q --timeout=15 --tries=3 "https://raw.githubusercontent.com/wptangtoc/wptangtoc-ols/refs/heads/main/wptangtoc-ols.zip.sig" -O wptangtoc-ols.zip.sig
    fi
    _rundone "Tải mã nguồn Core WPTangToc OLS"

    # 3. Tải File User (.zip) và File Chữ Ký (.sig)
    _runing "Đang tải mã nguồn User WPTangToc OLS"
    wget -q --timeout=15 --tries=3 "https://wptangtoc.com/share/wptangtoc-ols-user.zip" -O wptangtoc-ols-user.zip
    wget -q --timeout=15 --tries=3 "https://hub.wptangtoc.com/wptangtoc-ols-user.zip.sig" -U 'checksum wptangtoc ols' -O wptangtoc-ols-user.zip.sig
    
    # Dự phòng Github
    if [[ ! -s wptangtoc-ols-user.zip || ! -s wptangtoc-ols-user.zip.sig ]]; then
        wget -q --timeout=15 --tries=3 "https://raw.githubusercontent.com/wptangtoc/wptangtoc-ols/refs/heads/main/wptangtoc-ols-user.zip" -O wptangtoc-ols-user.zip
        wget -q --timeout=15 --tries=3 "https://raw.githubusercontent.com/wptangtoc/wptangtoc-ols/refs/heads/main/wptangtoc-ols-user.zip.sig" -O wptangtoc-ols-user.zip.sig
    fi
    _rundone "Tải mã nguồn User WPTangToc OLS"

    # ==========================================================================
    # QUY TRÌNH QUÉT CHỮ KÝ VÀ KIỂM ĐỊNH TOÀN VẸN
    # ==========================================================================
    _runing "Đang xác thực Chữ ký điện tử (GPG Signature)..."
    
    # Cờ xác nhận (Mặc định là Thất bại)
    auth_core=0
    auth_user=0

    # Kiểm tra File Core
    if [[ -f wptangtoc-ols.zip && -f wptangtoc-ols.zip.sig ]]; then
        if gpg --verify wptangtoc-ols.zip.sig wptangtoc-ols.zip 2>/dev/null; then
            auth_core=1
        fi
    fi

    # Kiểm tra File User
    if [[ -f wptangtoc-ols-user.zip && -f wptangtoc-ols-user.zip.sig ]]; then
        if gpg --verify wptangtoc-ols-user.zip.sig wptangtoc-ols-user.zip 2>/dev/null; then
            auth_user=1
        fi
    fi

    # Báo lỗi khẩn cấp nếu GPG phát hiện giả mạo
    if [[ $auth_core -eq 0 || $auth_user -eq 0 ]]; then
      _runloi "Xác thực GPG Không Thành Công"
      [[ -n "$wptangtocols_version" ]] && echo "$wptangtocols_version" >/tmp/wptangtoc-ols-version
      
      echo -e "\n${C_RED}╭──────────────────────────────────────────────────────────────────────────────╮${C_RESET}"
      echo -e "${C_RED}│${C_RESET} ${C_YELLOW}CẢNH BÁO BẢO MẬT NGHIÊM TRỌNG (GPG AUTHENTICATION FAILED)${C_RESET}                    ${C_RED}│${C_RESET}"
      echo -e "${C_RED}├──────────────────────────────────────────────────────────────────────────────┤${C_RESET}"
      echo -e "${C_RED}│${C_RESET} Hệ thống phát hiện mã nguồn tải về bị hỏng hoặc chữ ký không hợp lệ!       ${C_RED}│${C_RESET}"
      echo -e "${C_RED}│${C_RESET} Quá trình cập nhật đã bị HỦY BỎ để bảo vệ an toàn cho máy chủ của bạn.       ${C_RED}│${C_RESET}"
      echo -e "${C_RED}╰──────────────────────────────────────────────────────────────────────────────╯${C_RESET}\n"
      
      rm -rf "${TMP_DIR:?}"
      sleep 4
      [[ ${1:-} == "98" ]] && exec /etc/wptt/wptt-update-main 1
      [[ ${1:-} == "97" ]] && exec /usr/bin/wptangtoc 1
      exec /etc/wptt/wptt-status2
      exit 1
    fi
    
    _rundone "Xác thực GPG Thành Công. Đang tiến hành cài đặt..."

    # ==========================================================================
    # BACKUP ĐỂ ROLLBACK (PHÒNG HỜ)
    # ==========================================================================
    kiem_tra_ton_tai_nhom=$(cat /etc/group | grep '^wptangtoc-ols:')
    mkdir -p /etc/wptt/wptt-backup-system
    timedate=$(date +%Hgio_%d_%m_%Y)
    BACKUP_FILE="/etc/wptt/wptt-backup-system/wptt_backup_${timedate}_version:$version_wptangtoc_ols.zip"

    (cd /etc/wptt && zip -r -q "$BACKUP_FILE" . -x "wptt-backup-system/*" -x ".wptt.conf" -x ".ram_hien_tai" -x "vhost/*" -x "tmp/*")
    [[ -f "$BACKUP_FILE" ]] && zip -g -j -q "$BACKUP_FILE" /usr/bin/wptangtoc /usr/bin/wptt /usr/bin/wtop
    ls -dt /etc/wptt/wptt-backup-system/*.zip 2>/dev/null | tail -n +11 | xargs -r rm -f

    # TIẾN HÀNH GIẢI NÉN VÀ CHÉP FILE (CORE)
    unzip -oq wptangtoc-ols.zip
    if ! \cp -rf tool-wptangtoc-ols/* /etc/wptt/ 2>/dev/null; then
      echo -e "\n${C_RED}❌ Lỗi chép file! Đang tự động khôi phục lại phiên bản cũ...${C_RESET}"
      unzip -oq "$BACKUP_FILE" -d /etc/wptt/
      \cp -f /etc/wptt/wptangtoc /usr/bin
      rm -f /etc/wptt/wptangtoc
      \cp -f /etc/wptt/wptt /usr/bin
      rm -f /etc/wptt/wptt

      \cp -f /etc/wptt/tai-nguyen-server/wtop /usr/bin

      cd "$OLD_PWD" || cd /root || exit
      rm -rf "${TMP_DIR:?}"
      sleep 3
      [[ ${1:-} == "98" ]] && exec /etc/wptt/wptt-update-main 1
      [[ ${1:-} == "97" ]] && exec /usr/bin/wptangtoc 1
      exec /etc/wptt/wptt-status2
      exit
    fi

    rm -f wptangtoc-ols.zip wptangtoc-ols.zip.sig
    rm -rf tool-wptangtoc-ols

    [[ -d /etc/wptt-child ]] && \cp -rf /etc/wptt-child/* /etc/wptt

    chmod -R 700 /etc/wptt
    if [[ -n "$kiem_tra_ton_tai_nhom" ]]; then
      chown root:wptangtoc-ols /etc/wptt /etc/wptt/vhost
      chmod 750 /etc/wptt /etc/wptt/vhost
    else
      chown root:root /etc/wptt /etc/wptt/vhost
    fi

    \cp -f /etc/wptt/wptangtoc /usr/bin
    rm -f /etc/wptt/wptangtoc
    \cp -f /etc/wptt/wptt /usr/bin
    rm -f /etc/wptt/wptt

    \cp -f /etc/wptt/tai-nguyen-server/wtop /usr/bin


	# ==========================================================================
    # CẬP NHẬT PHÂN QUYỀN VHOST THEO CƠ CHẾ BẢO MẬT (ZERO TRUST)
    # ==========================================================================
	chown root:root /etc/wptt/.wptt.conf
	chmod 600 /etc/wptt/.wptt.conf

    for filepath in /etc/wptt/vhost/.*.conf; do
        [[ ! -f "$filepath" || "$filepath" == *"/..conf" ]] && continue
        
        # 1. Trạng thái mặc định: Đóng băng tuyệt đối (Auto-Healing)
        chown root:root "$filepath"
        chmod 600 "$filepath"

        # 2. Kiểm tra nới lỏng quyền nếu hệ thống có sử dụng Jail
        if [[ -n "$kiem_tra_ton_tai_nhom" ]]; then
            domain="${filepath##*/}"
            domain="${domain%.conf}"
            domain="${domain#.}"

            path="/usr/local/lsws/$domain"

            # Xác định lồng Jail đang hoạt động (có thư mục /etc của root)
            if [[ -d "$path" && -d "$path/etc" ]]; then
                if [[ $(stat -c '%U' "$path/etc" 2>/dev/null) == "root" ]]; then
                    # Dùng grep bóc tách biến an toàn
                    check_user_vhost=$(grep -E '^User_name_vhost=' "$filepath" | cut -d'=' -f2 | tr -d '"' | tr -d "'")
                    
                    if [[ -n "$check_user_vhost" ]]; then
                        # Kiểm tra user có nằm trong nhóm wptangtoc-ols không
                        if grep -q "^wptangtoc-ols:.*$check_user_vhost" /etc/group 2>/dev/null; then
                            chown root:wptangtoc-ols "$filepath"
                            chmod 640 "$filepath"
                        fi
                    fi
                fi
            fi
        fi
    done



    # ==========================================================================
    # TIẾN HÀNH GIẢI NÉN VÀ CHÉP FILE (USER)
    # ==========================================================================
    unzip -qo wptangtoc-ols-user.zip
    \cp -rf tool-wptangtoc-ols-user/* /etc/wptt-user

    rm -f wptangtoc-ols-user.zip wptangtoc-ols-user.zip.sig
    rm -rf tool-wptangtoc-ols-user

    if [[ -n "$kiem_tra_ton_tai_nhom" ]]; then
      chown -R root:wptangtoc-ols /etc/wptt-user
      chmod -R 750 /etc/wptt-user
      \cp -f /etc/wptt-user/wptangtoc-user /usr/bin
      chown root:wptangtoc-ols /usr/bin/wptangtoc-user
      chmod 750 /usr/bin/wptangtoc-user
    else
      chown -R root:root /etc/wptt-user
      chmod -R 700 /etc/wptt-user
      \cp -f /etc/wptt-user/wptangtoc-user /usr/bin
      chown root:root /usr/bin/wptangtoc-user
      chmod 700 /usr/bin/wptangtoc-user
    fi

	chmod 700 /etc/wptt/tmp

    # Khử dấu ngôn ngữ
    if [[ "$khong_dau_vi_ngon_ngu" == '1' ]]; then
      find /etc/wptt -type f ! -path '/etc/wptt/lang/wptt-set-lang-vi' ! -path '/etc/wptt/wptt-update2' ! -path '/etc/wptt/wptt-update' ! -name '.*' -exec sed -i 'y/áàảãạăắằẳẵặâấầẩẫậđéèẻẽẹêếềểễệíìỉĩịóòỏõọôốồổỗộơớờởỡợúùủũụưứừửữựýỳỷỹỵÁÀẢÃẠĂẮẰẲẴẶÂẤẦẨẪẬĐÉÈẺẼẸÊẾỀỂỄỆÍÌỈĨỊÓÒỎÕỌÔỐỒỔỖỘƠỚỜỞỠỢÚÙỦŨỤƯỨỪỬỮỰÝỲỶỸỴ/aaaaaaaaaaaaaaaaadeeeeeeeeeeeiiiiiooooooooooooooooouuuuuuuuuuuyyyyyAAAAAAAAAAAAAAAAADEEEEEEEEEEEIIIIIOOOOOOOOOOOOOOOOOUUUUUUUUUUUYYYYY/' {} +
      [[ -n "$kiem_tra_ton_tai_nhom" ]] && sudo find /etc/wptt-user -type f -exec sed -i 'y/áàảãạăắằẳẵặâấầẩẫậđéèẻẽẹêếềểễệíìỉĩịóòỏõọôốồổỗộơớờởỡợúùủũụưứừửữựýỳỷỹỵÁÀẢÃẠĂẮẰẲẴẶÂẤẦẨẪẬĐÉÈẺẼẸÊẾỀỂỄỆÍÌỈĨỊÓÒỎÕỌÔỐỒỔỖỘƠỚỜỞỠỢÚÙỦŨỤƯỨỪỬỮỰÝỲỶỸỴ/aaaaaaaaaaaaaaaaadeeeeeeeeeeeiiiiiooooooooooooooooouuuuuuuuuuuyyyyyAAAAAAAAAAAAAAAAADEEEEEEEEEEEIIIIIOOOOOOOOOOOOOOOOOUUUUUUUUUUUYYYYY/' {} +
    fi

    # ==========================================================================
    # Cấu hình phụ trợ (Script Update server & Dự phòng Github bọc GPG)
    # ==========================================================================
    # 1. Tải file update và file chữ ký số
    curl -sO https://wptangtoc.com/share/update --connect-timeout 10 --max-time 15
    curl -sO https://hub.wptangtoc.com/update.sig --connect-timeout 10 --max-time 15 -A "checksum wptangtoc ols"
    
    # 2. Dự phòng nếu tải thất bại hoặc file trống
    if [[ ! -s update || ! -s update.sig ]]; then
      curl -sO https://raw.githubusercontent.com/wptangtoc/wptangtoc-ols/refs/heads/main/update --connect-timeout 10 --max-time 15
      curl -sO https://raw.githubusercontent.com/wptangtoc/wptangtoc-ols/refs/heads/main/update.sig --connect-timeout 10 --max-time 15
    fi

    # 3. Quét GPG và chỉ thực thi khi 100% chính chủ
    if [[ -s update && -s update.sig ]] && gpg --verify update.sig update 2>/dev/null; then
      if grep -q 'UPDATE_DONE="YES"' update 2>/dev/null; then
        bash update >/dev/null 2>&1
      fi
    else
      # Tùy chọn: Ghi log ngầm nếu phát hiện file bị can thiệp
      echo "Lỗi: Script 'update' sai chữ ký GPG hoặc rớt mạng - Bỏ qua thực thi ( $(date '+%d-%m-%Y %H:%M') )" >> /var/log/wptangtoc-ols.log
    fi
    
    # 4. Dọn rác
    rm -f update update.sig

    # Lưu Version
    sed -i "/version_wptangtoc_ols/d" /etc/wptt/.wptt.conf
    echo "version_wptangtoc_ols=$wptangtocols_version" >>/etc/wptt/.wptt.conf
    echo "$wptangtocols_version" >/tmp/wptangtoc-ols-version
    echo "$version_wptangtoc_ols" >/tmp/wptangtoc-ols-version-main

    # Dọn dẹp
    cd "$OLD_PWD" || cd /root || exit
    rm -rf "${TMP_DIR:?}"

    # Thông báo hoàn tất
    _rundone "WPTangToc OLS đã cập nhật Hệ thống lên bản $wptangtocols_version"

    echo "Cập nhật WPTangToc OLS: $(date '+%d-%m-%Y %H:%M')" >>/var/log/wptangtoc-ols.log
    sleep 3

    if [[ -n "${1:-}" ]]; then
      [[ "${1:-}" == "98" ]] && exec /etc/wptt/wptt-update-main
      [[ "${1:-}" == "97" ]] && exec /usr/bin/wptangtoc 1
    fi
  else
    # Nếu người dùng bấm "Không đồng ý / Để sau"
    echo -e "${C_YELLOW}Đã hủy thao tác cập nhật.${C_RESET}"
    sleep 1
  fi

  if [[ -n "${1:-}" ]]; then
    [[ "${1:-}" == "98" ]] && exec /etc/wptt/wptt-update-main
    [[ "${1:-}" == "97" ]] && exec /usr/bin/wptangtoc 1
  fi
  exec /etc/wptt/wptt-status2
fi
