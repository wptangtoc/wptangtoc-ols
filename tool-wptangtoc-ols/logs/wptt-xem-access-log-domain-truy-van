#!/bin/bash
# @author: Gia Tuấn
# @website: https://wptangtoc.com
# @since: 2025

function huong_dan() {
  Việc xem access log cho một domain cụ thể thông qua WPTangToc OLS thường sẽ tận dụng các công cụ sẵn có của hệ điều hành Linux.

  Tính năng xem access log domain trong WPTangToc OLS có thể được hiểu ngắn gọn như sau:

  Xác định vị trí file log: OpenLiteSpeed thường lưu access log cho mỗi virtual host [tương ứng với mỗi domain] trong một file riêng biệt. Đường dẫn phổ biến có thể là /usr/local/lsws/domain.com/logs/access.log. Script cung cấp một công cụ tìm kiếm siêu tốc [FZF] để truy vấn nhanh vào file log này.

  Tính năng này hỗ trợ người dùng dễ dàng lọc, tìm kiếm IP, mã trạng thái [như 404, 500], hoặc URL truy cập. Điều này giúp quản trị viên website nhanh chóng kiểm tra lưu lượng truy cập, phát hiện lỗi hoặc các hoạt động bất thường [như DDoS, Scan lỗi] liên quan đến một website cụ thể.
}

. /etc/wptt/.wptt.conf
[[ -z "$ngon_ngu" ]] && ngon_ngu='vi'
. /etc/wptt/lang/$ngon_ngu.sh
. /etc/wptt/echo-color

clear
echo -e "\n========================================================================="
echo -e "| Quản lý Logs => Truy vấn Access Log Domain [Tìm Kiếm Siêu Tốc]        |"
echo -e "=========================================================================\n"

# Kiểm tra yêu cầu hệ thống
if ! command -v fzf &>/dev/null; then
  echoDo "Lỗi: Hệ thống của bạn chưa cài đặt công cụ FZF!"
  echo "Vui lòng cài đặt FZF để sử dụng tính năng truy vấn siêu tốc này."
  sleep 3
  exec /etc/wptt/wptt-logs-main 1
fi

# ==============================================================================
# HÀM MENU LỌC THÔNG MINH (CÓ TRẠNG THÁI + FZF)
# ==============================================================================
function lua_chon_NAME() {
  unset NAME domain selects
  selects=()

  local file_list=()
  [[ -d /etc/wptt/vhost ]] && file_list+=(/etc/wptt/vhost/.*.conf)
  [[ -d /etc/wptt/chuyen-huong ]] && file_list+=(/etc/wptt/chuyen-huong/*)

  for filepath in "${file_list[@]}"; do
    [[ ! -f "$filepath" || "$filepath" == *"/..conf" || "$filepath" == *"*"* ]] && continue

    local domain="${filepath##*/}"
    domain="${domain%.conf}"
    domain="${domain#.}"

    if [[ "$domain" == ?*.?* ]]; then
      local vhost_conf="/usr/local/lsws/conf/vhosts/$domain/$domain.conf"
      if [[ -f "$vhost_conf" ]]; then
        if grep -q 'accesslog ' "$vhost_conf" 2>/dev/null; then
          selects+=("$domain [Đang BẬT Log]")
        else
          selects+=("$domain [Đang TẮT]")
        fi
      fi
    fi
  done

  if [[ ${#selects[@]} -eq 0 ]]; then
    echo -e "\n\033[1;33mKhông tìm thấy website nào trên hệ thống để thao tác!\033[0m"
    sleep 3
    exec /etc/wptt/wptt-logs-main 1
  fi

  mapfile -t selects < <(printf "%s\n" "${selects[@]}" | sort -uV)

  if [[ ${#selects[@]} -ge 2 ]]; then
    selects+=("TẤT CẢ WEBSITE (Không hỗ trợ mục này)") # Thêm cho chuẩn form, nhưng sẽ chặn nếu user chọn
  fi

  PS3="$(tput setab 0)-//- Lựa chọn website muốn truy vấn Log [00=Tìm nhanh] [0=Thoát]:$(tput sgr0) "

  select selected_domain in "${selects[@]}"; do
    NAME="$selected_domain"
    break
  done

  # XỬ LÝ CHỌN NHANH [00] VỚI FZF
  if [[ "$REPLY" == "00" ]]; then
    local selected_name
    selected_name=$(printf "%s\n" "${selects[@]}" | nl -w 3 -s ': ' | fzf --height 40% --border=rounded --color=border:red --reverse --prompt=">> Tìm website truy vấn Log [0=Thoát]: ")
    selected_name=$(echo "$selected_name" | sed 's/^[[:space:]]*[0-9]\+\:[[:space:]]*//')

    if [[ -z "$selected_name" ]]; then
      echo "Đã hủy chọn."
      exec /etc/wptt/wptt-logs-main 1
    fi
    NAME="$selected_name"
  fi

  if [[ -z "$NAME" && "$REPLY" != "0" && "$REPLY" != "00" ]]; then
    REPLY=$(echo "$REPLY" | tr '[:upper:]' '[:lower:]' | xargs)
    for item in "${selects[@]}"; do
      local check_dom=$(echo "$item" | cut -f1 -d ' ')
      if [[ "$check_dom" == "$REPLY" ]]; then
        NAME="$item"
        break
      fi
    done
  fi
}

# Nếu gọi script kèm tên domain từ command line
if [[ -n "$1" && "$1" != "98" ]]; then
  NAME="$1"
else
  lua_chon_NAME
fi

# Tách lấy tên chuẩn
NAME=$(echo "$NAME" | cut -f1 -d ' ')

# Chốt chặn thoát hiểm và lựa chọn sai
[[ "$NAME" == "0" || -z "$NAME" ]] && exec /etc/wptt/wptt-logs-main 1
[[ "$NAME" == "TẤT" ]] && {
  echoDo "Chức năng này chỉ hỗ trợ truy vấn từng website cụ thể!"
  sleep 2
  exec /etc/wptt/wptt-logs-main 1
}

LOG_DIR="/usr/local/lsws/$NAME/logs"
LOG_FILE="$LOG_DIR/access.log"
VHOST_CONF="/usr/local/lsws/conf/vhosts/$NAME/$NAME.conf"

# ==============================================================================
# KIỂM TRA VÀ KÍCH HOẠT LOG NẾU CHƯA CÓ
# ==============================================================================
if [[ ! -d "$LOG_DIR" || ! -f "$LOG_FILE" ]]; then
  echo -e "\033[1;33mWebsite $NAME chưa được bật tính năng ghi Access Log!\033[0m"
  echo "Tính năng này mặc định tắt để bảo vệ hiệu suất CPU/Ổ cứng."
  echo ""

  prompt="Bạn có muốn BẬT ghi log cho website $NAME ngay bây giờ không? [1-2]: "
  options=("$dong_y" "$khong_dong_y")
  dongy="n"
  PS3="$prompt"
  select opt in "${options[@]}"; do
    case "$REPLY" in
    1)
      dongy="y"
      break
      ;;
    2) exec /etc/wptt/wptt-logs-main 1 ;;
    *)
      echo -e "\n$ban_nhap_sai_he_thong_se_chon_khong_dong_y\n"
      break
      ;;
    esac
  done

  if [[ "$dongy" == "y" ]]; then
    _runing "Kích hoạt ghi Access Log cho domain $NAME"

    if grep -qi "ubuntu" /etc/os-release 2>/dev/null; then
      tuong_thich_nhom_litespeed="nogroup"
    else
      tuong_thich_nhom_litespeed="nobody"
    fi

    # Ghi cấu hình thẳng vào Vhost an toàn
    sed -i -e '/^errorlog /,/^}$/d' "$VHOST_CONF" 2>/dev/null
    sed -i -e '/^accesslog /,/^}$/d' "$VHOST_CONF" 2>/dev/null
    cat <<EOF >>"$VHOST_CONF"

errorlog \$VH_ROOT/logs/error.log {
  useServer               1
  logLevel                ERROR
  rollingSize             10M
}

accesslog \$VH_ROOT/logs/access.log {
  useServer               0
  rollingSize             10M
  keepDays                30
  compressArchive         1
}
EOF

    mkdir -p "$LOG_DIR"
    chown -R nobody:$tuong_thich_nhom_litespeed "$LOG_DIR"
    chmod 700 "$LOG_DIR"

    /usr/local/lsws/bin/lswsctrl reload >/dev/null 2>&1

    # Ép OLS sinh file log ngay lập tức bằng 1 request
    curl -s -k "http://127.0.0.1" -H "Host: $NAME" >/dev/null 2>&1

    [[ -f "/etc/wptt/vhost/.$NAME.conf" ]] && sed -i '/bat_log_domain/d' "/etc/wptt/vhost/.$NAME.conf" && echo 'bat_log_domain=1' >>"/etc/wptt/vhost/.$NAME.conf"

    _rundone "Kích hoạt ghi Access Log cho domain $NAME"
    sleep 1
  fi
fi

if [[ ! -f "$LOG_FILE" ]]; then
  echoDo "Lỗi: Không tìm thấy file access.log! Vui lòng kiểm tra lại cấu hình."
  sleep 3
  exec /etc/wptt/wptt-logs-main 1
fi

# ==============================================================================
# GỌI FZF ĐỂ HIỂN THỊ VÀ TÌM KIẾM LOGS
# ==============================================================================
# Sử dụng tac để đọc log từ mới nhất đến cũ nhất
tac "$LOG_FILE" |
  fzf --prompt=">> Tìm kiếm Access Log [$NAME]: " \
    --height='~90%' \
    --border=rounded \
    --color=border:red \
    --layout=reverse \
    --info=inline \
    --header="Gõ từ khóa (IP, URL, 404, 500...) để lọc | Lên/Xuống để xem | ESC để thoát" \
    --preview="echo {}" \
    --preview-window=up:30%:wrap

# Sau khi tắt FZF (Bấm ESC hoặc Enter), trở về Menu chính
[[ "${1:-}" == "98" ]] && exec /etc/wptt/wptt-logs-main 1
