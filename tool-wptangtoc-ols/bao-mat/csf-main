#!/bin/bash
# @author: Gia Tuấn
# @website: https://wptangtoc.com
# @email: giatuan@wptangtoc.com
# @since: 2026

function huong_dan() {
  Tính năng quản lý CSF [ConfigServer Security & Firewall] trong WPTangToc OLS là một công cụ tích hợp giúp bạn dễ dàng cài đặt, cấu hình và điều khiển firewall CSF ngay từ giao diện hoặc dòng lệnh của WPTangToc OLS. 🛡️

  Nó đơn giản hóa việc bảo vệ máy chủ của bạn bằng cách cho phép thực hiện nhanh các tác vụ như:

  * Cài đặt và gỡ bỏ CSF: Triển khai hoặc loại bỏ firewall một cách thuận tiện.
  * Quản lý quy tắc IP: Dễ dàng cho phép [allow], chặn [deny/block], hoặc bỏ chặn [unblock] các địa chỉ IP, chặn IP theo quốc gia...
  * Mở/Đóng cổng [Ports]: Cấu hình các cổng được phép hoặc bị chặn trên máy chủ.
  * Kiểm soát dịch vụ CSF: Khởi động, dừng, hoặc khởi động lại dịch vụ firewall.
  * Xem trạng thái và logs: Nhanh chóng kiểm tra tình trạng hoạt động của firewall và xem nhật ký liên quan.
  * v.v.v
  Về cơ bản, tính năng này giúp bạn quản lý một trong những firewall mạnh mẽ và phổ biến cho Linux một cách trực quan và hiệu quả hơn, tăng cường an ninh cho máy chủ của bạn.
}

# ==============================================================================
# NẠP CẤU HÌNH CƠ BẢN & GIAO DIỆN
# ==============================================================================
if [[ -z "$1" ]]; then
  . /etc/wptt/wptt-header-menu
  header_menu
else
  clear
  . /etc/wptt/echo-color 2>/dev/null
  echo -e "${C_CYAN}╭──────────────────────────────────────────────────────────────────────────────╮${C_RESET}"
  
  # Hàm vẽ tiêu đề phụ lọt lòng 78 (chống lệch UTF-8)
  center_tmp() {
    local text="$1"
    local clean_len=$(echo -en "$text" | sed "s/\x1B\[[0-9;]*[a-zA-Z]//g" | tr -d '\r\n')
    local len=$(LC_ALL=C.UTF-8 printf "%s" "$clean_len" | wc -m | tr -d ' ' 2>/dev/null)
    [[ -z "$len" || "$len" == "0" ]] && len=${#clean_len}
    
    local pad_left=$(((78 - len) / 2))
    local pad_right=$((78 - len - pad_left))
    printf "${C_CYAN}│${C_RESET}%*s%b%*s${C_CYAN}│${C_RESET}\n" "$pad_left" "" "$text" "$pad_right" ""
  }
  
  center_tmp "${C_YELLOW}TRUNG TÂM QUẢN LÝ TƯỜNG LỬA (CSF FIREWALL)${C_RESET}"
  echo -e "${C_CYAN}╰──────────────────────────────────────────────────────────────────────────────╯${C_RESET}"
fi

. /etc/wptt/.wptt.conf 2>/dev/null
[[ -z "$ngon_ngu" ]] && ngon_ngu='vi'
. /etc/wptt/lang/$ngon_ngu.sh 2>/dev/null

echo ""
echo "$quan_ly_csf:" > /dev/null # Dữ liệu ẩn cho bộ tìm kiếm WPTangToc

# ==============================================================================
# TẠO MENU FULL 1 CỘT ĐỂ TRÁNH TRÀN CHỮ
# ==============================================================================
MENU_TITLE="TRUNG TÂM QUẢN LÝ CSF"

options=(
  "$kich_hoat_webguiadmin CSF"
  "$huy_kich_hoat_webguiadmin CSF"
  "Chặn quốc gia"
  "Chỉ cho phép một số quốc gia được truy cập"
  "Hủy bỏ chặn quốc gia"
  "Hủy bỏ chặn toàn bộ quốc gia chỉ được một số quốc gia cho phép"
  "Bật chế độ chống DDOS"
  "Tắt chế độ chống DDOS"
)

# Hàm thực thi hành động
run_action() {
  local index=$1
  local script_path=""
  local script_args="98"

  case $index in
  0) script_path="/etc/wptt/bao-mat/csf/wptt-cai-dat-csf" ;;
  1) script_path="/etc/wptt/bao-mat/csf/wptt-huy-cai-dat-csf" ;;
  2) script_path="/etc/wptt/bao-mat/csf/chan-ip-quoc-gia" ;;
  3) script_path="/etc/wptt/bao-mat/csf/chan-ip-quoc-gia-chi-cho-phep" ;;
  4) script_path="/etc/wptt/bao-mat/csf/huy-bo-chan-ip-quoc-gia" ;;
  5) script_path="/etc/wptt/bao-mat/csf/huy-bo-chan-ip-quoc-gia-chi-cho-phep" ;;
  6) script_path="/etc/wptt/bao-mat/csf/mod-csf-chong-ddos" ;;
  7) script_path="/etc/wptt/bao-mat/csf/tat-mod-csf-chong-ddos" ;;
  *)
    echo -e "\n${C_RED}[!] ${lua_chon_khong_hop_le:-Lựa chọn không hợp lệ}: Index $index.${C_RESET}"
    return 1
    ;;
  esac

  # Kiểm tra và chạy script con
  if [[ -x "$script_path" || -f "$script_path" ]]; then
    exec bash "$script_path" "$script_args"
  else
    echo -e "\n${C_RED}[!] Lỗi: Không tìm thấy kịch bản '$script_path'.${C_RESET}"
    sleep 2
    exec /etc/wptt/csf-main 1
  fi
}

# ==============================================================================
# TÙY BIẾN GIAO DIỆN HIỂN THỊ FULL 1 CỘT (NÚT MÀU VÀNG & CHỐNG LỆCH UTF-8)
# ==============================================================================
print_full_row() {
  local index="$1"
  local text="$2"
  
  # Format cho index (vd: " 1", " 2") để thẳng hàng
  local formatted_index=$(printf "%2s" "$index")
  
  # Chuỗi thô dùng để đếm ký tự (không chứa mã màu)
  local raw_text="  [${formatted_index}] ${text}"
  local clean_text=$(echo -en "$raw_text" | sed "s/\x1B\[[0-9;]*[a-zA-Z]//g" | tr -d '\r\n')
  
  # Đếm bằng UTF-8 để tiếng Việt không bóp méo khung
  local text_len=$(LC_ALL=C.UTF-8 printf "%s" "$clean_text" | wc -m | tr -d ' ' 2>/dev/null)
  [[ -z "$text_len" || "$text_len" == "0" ]] && text_len=${#clean_text}
  
  # Tính toán khoảng trống còn lại để đập vào lề phải (78 lọt lòng - 1 space lề)
  local pad=$((78 - text_len - 1))
  [[ $pad -lt 0 ]] && pad=0
  
  # In ra với mã màu VÀNG ($C_YELLOW) bọc đúng cặp ngoặc và số
  echo -e "${C_CYAN}│${C_RESET}  ${C_YELLOW}[${formatted_index}]${C_RESET} ${text}$(printf '%*s' "$pad" "") ${C_CYAN}│${C_RESET}"
}

draw_custom_grid() {
  # Duyệt qua mảng options và in ra từng dòng
  for i in "${!options[@]}"; do
    print_full_row "$((i + 1))" "${options[$i]}"
  done
}

# Kích hoạt Engine Menu
. /etc/wptt/wptt-callback-menu-chon
