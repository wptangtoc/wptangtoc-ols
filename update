#!/bin/bash
echo "Cảm ơn bạn đã sử dụng WPTangToc OLS"


if [[ $(which dnf 2>/dev/null) ]];then
if [[ -d /etc/wptt/.cloudflare ]];then
yum install -y python3-cloudflare python3-certbot-dns-cloudflare >/dev/null 2>&1
fi
fi

