#!/bin/bash
function lua_chon_NAME() {
	NAME=""
	if [ "$(ls -At ~/)" ]; then
		selects=()
		for entry in $(ls -A ~/); do
			NAME=$(echo $entry | grep -Po '.*?//\K.*?(?=/)')
			if [ "$NAME" != "${NAME/./}" ]; then
				selects+=("$NAME")
			fi
		done
		PS3="
-//- Nhập lựa chọn website của bạn [0=Thoát]: "
		select select in ${selects[@]}; do
			NAME=$select
			index=$REPLY
			break
		done
	else
		clear
		echo "Không có domain nào tồn tại trên hệ thống."
		exit
	fi
}
